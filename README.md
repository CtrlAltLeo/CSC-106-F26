# CSC-106-F26

# Pyenv - Python Version Manager

```bash
#!/bin/bash
curl https://pyenv.run | bash
~/.pyenv/bin/pyenv install 3.14.7

```

# First Setup

1. Click on the green `Code` button, and select HTTPS
2. Copy the link. Open the terminal, and type `git clone <link you copied>`
3. If asked, authenticate with Github using the browser and token

# Creating a branch

1. `cd` into the folder
2. `git branch lastname-firstname`
3. `git checkout lastname-firstname`

# Committing Changes

1. `git add .`
2. git commit -m <message>

# Pushing to remote

# Making a Pull Request


